// Impostazioni generiche per RTK Query / UI
export const PLC_PAGINATION = {
  DEFAULT_PAGE_SIZE: 10,
};

export const PLC_API_CONFIG = {
  CACHE_TIME: 60, // seconds
  RETRY_ATTEMPTS: 0,
  POLLING_INTERVAL: 30_000,
  DEBOUNCE_DELAY: 300,
};
